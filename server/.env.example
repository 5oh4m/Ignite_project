# Backend Environment Variables
# Add these in Vercel dashboard or using Vercel CLI

# MongoDB Connection String (use MongoDB Atlas for production)
MONGODB_URI=mongodb+srv://username:password@cluster.xxxxx.mongodb.net/medlink?retryWrites=true&w=majority

# JWT Secret Key (generate a strong random string)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Node Environment
NODE_ENV=production

# Server Port (Vercel handles this automatically)
PORT=5001
